import React from 'react';
import PropTypes from 'prop-types';

const CircleGroupButton = (props) => {
    return (
        <div></div>
    );
};

CircleGroupButton.propTypes = {};
CircleGroupButton.defaultProps = {};

export default CircleGroupButton;
