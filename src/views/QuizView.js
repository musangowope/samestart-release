import React from 'react';
import PropTypes from 'prop-types';

const QuizView = (props) => {
  return <div>Quiz View</div>;
};

QuizView.propTypes = {};
QuizView.defaultProps = {};

export default QuizView;
