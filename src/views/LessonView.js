import React from 'react';
import PropTypes from 'prop-types';

const LessonView = (props) => <div>Lesson View</div>;

LessonView.propTypes = {};
LessonView.defaultProps = {};

export default LessonView;
